#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main()
{
    /*

    char nome[50];
    int tam;

    printf("Digite um nome\n");
    scanf("%s", nome);

    tam = strlen(nome);

    if (tam>=5 && tam<=10){

        printf("%s", nome);
    }
    */

    /*
    char nome1[50], nome2[50];

    int ret;

    printf("Digite um nome\n");
    scanf("%s", nome1);

    printf("Digite outro nome\n");
    scanf("%s", nome2);

    ret = strcomp(nome1, nome2);

    if (ret == 0)
        printf("%s é igual a %s", nome1, nome2);
    if (ret > 0)
        printf("%s é maior que %s", nome1, nome2);
    if (ret < 0)
        printf("%s é menor que %s", nome1, nome2);

    */

    char nome[50];
    char c = 'a';

    strcpy(nome, "pedro");

    printf("%s\n", nome);

    strcat(nome, " Firmino");

    printf("%s\n", nome);

    printf("c", toupper(cs));




    return 0;
}
